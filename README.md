# Homebrew for BodhiSearch apps

This repo contains up-to-date formulas to install BodhiSearch apps using Homebrew.

# Install Bodhi

1. Add `BodhiSearch/apps` as a tap into homebrew
  ```shell
  brew tap BodhiSearch/apps
  ```
2. Install `Bodhi`
  ```shell
  brew install bodhi
  ```
